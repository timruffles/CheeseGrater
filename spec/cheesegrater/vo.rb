root = File.dirname(__FILE__) 
require root + '/spec_helper'

describe CheeseGrater::Vo do
  
  it "exposes a fields hash"
  it "exposes its related hash"
  it "exposes its name"
  it "exposes its handler's name"
  
end