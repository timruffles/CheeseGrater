csv:
    format: querystring
    endpoint: http://blah.com
    fields:
        country: GB
        keywords:
            type: csv
            value: [hippo, mustapha, ghandi]
csv2:
    format: querystring
    endpoint: http://www.eventbrite.com/json/event_search
    fields:
        country: GB
        keywords:
            type: csv
            value: [one, two, three]
one_per_request:
  format: querystring
  endpoint: http://blah.com
  fields:
      country: GB
      keywords:
          type: one_per_request
          value: [hippo, mustapha, ghandi]
multiple_one_per_request:
    format: querystring
    endpoint: http://blah.com
    fields:
        country: GB
        keywords:
            type: one_per_request
            value: [hippo, mustapha, ghandi]
        type:
            type: one_per_request
            value: [one, two, three]
